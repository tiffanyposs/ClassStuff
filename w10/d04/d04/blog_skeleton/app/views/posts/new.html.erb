<!-- using form helpers, create a form that has a name of post and an action of create. there should be an input for title, author, and content. don't forget a submit button! after the form, create a cancel button that links back to index. -->


<%= form_for @post, url: {action: 'create'} do |f| %>
<h2><%= f.label :title %></h2>
<%= f.text_field :title %>

<h2><%= f.label :author %></h2>
<%= f.text_field :author %>
<br /><br />

<h2><%= f.label :content %></h2>
<%= f.text_area :content %>
<br>

<%= f.submit %>
<% end %>

<%= link_to 'Cancel', posts_path %>